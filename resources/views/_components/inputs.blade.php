<div>
    <label class={{ $labelClass }} for={{ $for }}> 
        {{ $labelName }} 
    </label>
    <input id={{ $inputId }}
    class={{ $inputClass }}
    type={{ $inputType }} 
    name={{ $inputName }} 
    value={{ $inputValue }} 
    autocomplete={{ $inputACompt }}
    placeholder={{ $inputPlcHdr }} />
    <br>
</div>